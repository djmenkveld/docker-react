language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t djmenkveld/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true djmenkveld/docker-react npm run test
  